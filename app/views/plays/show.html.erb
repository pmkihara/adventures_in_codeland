<div class="play-container" data-controller="play" data-play-info-value="<%= @play_infos.to_json %>">
  <div class="map-view"> <%# Sets a layer for the map %>
    <div class="dialogue-layer"> <%# Sets a layer above the map for the dialogues %>
      <%= render 'dialogue' %>
    </div>
      <%= render'map' %>
  </div>
</div>
